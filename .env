# ============ DOCKER COMPOSE ENV ============
# Переменные для docker-compose.yml

# Пароль для PostgreSQL (используется в docker-compose)
DB_PASSWORD=password

# JWT Secret (минимум 32 символа)
JWT_SECRET=city-delivery-super-secret-jwt-key-2024-change-in-production

