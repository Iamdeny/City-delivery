# URL веб-приложения (Next.js). Для теста на телефоне — IP ПК в локальной сети.
EXPO_PUBLIC_WEB_URL=http://192.168.1.100:3000

# Появится после `npx eas-cli init` в папке mobile (опционально в .env)
# EAS_PROJECT_ID=
