/**
 * Единая точка экспорта типов
 * Все типы должны импортироваться отсюда для соблюдения DRY принципа
 */
export type { Product, ProductFilters } from '../../entities/product/model/types';
export type { CartItem } from '../../entities/cart/model/schema';
